import React from 'react'

/**
 * DSTagPill - Tag pill component for displaying tags
 * 
 * @param {React.ReactNode} children - Tag text/content
 */
export function DSTagPill({ children }) {
  return <span className="ds-tag-pill">{children}</span>
}

